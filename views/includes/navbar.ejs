<nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm py-2">
  <div class="container-fluid px-lg-5">
    
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/index">
      <i class="fa-solid fa-handshake fa-lg text-success"></i>
      <span class="fw-bold text-success">ProGuide</span>
    </a>

    <!-- Toggler -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Content -->
    <div class="collapse navbar-collapse" id="navbarContent">
      
     <!-- Centered Search Bar -->
    <div class="mx-auto w-100 px-3 px-lg-5 my-3 my-lg-0" style="max-width: 550px;">
      <% if (currUser && (currUser.role === "Mentee" || currUser.role === "Mentor")) { %>
        <form method="GET" action="/mentors" class="d-flex gap-2 align-items-center">
          <div class="flex-grow-1">
            <input 
              class="form-control rounded-pill py-2 px-3" 
              name="skill" 
              type="search" 
              placeholder="Search services..." 
              aria-label="Search"
              style="height: 45px;"
            >
          </div>
          <div>
            <button 
              class="btn btn-success rounded-pill px-4 py-2" 
              type="submit"
              style="height: 45px;"
            >
              Search
            </button>
          </div>
        </form>
      <% } else if (currUser && (currUser.role === "Freelancer" || currUser.role === "Employer")) { %>
        <form method="GET" action="/joblist" class="d-flex gap-2 align-items-center">
          <div class="flex-grow-1">
            <input 
              class="form-control rounded-pill py-2 px-3" 
              name="skill" 
              type="search" 
              placeholder="Search jobs..." 
              aria-label="Search"
              style="height: 45px;"
            >
          </div>
          <div>
            <button 
              class="btn btn-success rounded-pill px-4 py-2" 
              type="submit"
              style="height: 45px;"
            >
              Search
            </button>
          </div>
        </form>
      <% } else { %>
        <form method="GET" action="/search" class="d-flex gap-2 align-items-center">
          <div class="flex-grow-1">
            <input 
              class="form-control rounded-pill py-2 px-3" 
              name="query" 
              type="search" 
              placeholder="Search..." 
              aria-label="Search"
              style="height: 45px;"
            >
          </div>
          <div>
            <button 
              class="btn btn-success rounded-pill px-4 py-2" 
              type="submit"
              style="height: 45px;"
            >
              Search
            </button>
          </div>
        </form>
      <% } %>
    </div>


      <!-- Right Actions -->
      <ul class="navbar-nav ms-auto align-items-center gap-2">
        <% if (!currUser) { %>
          <li class="nav-item">
            <a class="btn btn-outline-success px-3" href="/register">
              <i class="bi bi-person-plus-fill me-1"></i> Register
            </a>
          </li>
          <li class="nav-item">
            <a class="btn btn-success px-3" href="#" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
              <i class="bi bi-box-arrow-in-right me-1"></i> Login
            </a>
          </li>
        <% } else { %>

          <% if (currUser.role === "Employer") { %>
          <li class="nav-item">
            <button class="btn btn-outline-success px-3" data-bs-toggle="offcanvas" data-bs-target="#jobApplications">
              <i class="bi bi-briefcase me-1"></i> Applications
            </button>
          </li>
          <% } %>

          <li class="nav-item">
            <% if (showCreateProfile && currUser.role === "Mentor") { %>
              <a class="btn btn-outline-success px-3" href="/createProfile">Create Profile</a>
            <% } else if (currUser.role === "Mentor") { %>
              <a class="btn btn-outline-success px-3" href="/createProfile/<%= mentorProfile.id %>/editProfile">Edit Profile</a>
            <% } else if (currUser.role === "Employer") { %>
              <a class="btn btn-outline-success px-3" href="/jobs/new">Post Job</a>
            <% } %>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
              <img src="<%= currUser?.profilePicture?.url || '/default-avatar.png' %>" alt="Avatar"
                   class="rounded-circle" width="32" height="32" style="object-fit: cover;">
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <% if (currUser.role === "Mentor") { %>
                <li><a class="dropdown-item" href="/showProfile">Dashboard</a></li>
              <% } %>
              <% if (currUser.role === "Employer") { %>
                <li><a class="dropdown-item" href="/myJobs">My Jobs</a></li>
              <% } %>
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>


  <!-- Bootstrap Offcanvas for Job Applications -->
<div class="offcanvas offcanvas-end" tabindex="-1" id="jobApplications">
  <div class="offcanvas-header border-bottom">
      <h5 class="offcanvas-title">
          <i class="bi bi-briefcase me-2"></i>Job Applications
      </h5>
      <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
  </div>
  <div class="offcanvas-body">
      <% if (jobApplications.length > 0) { %>
          <div class="list-group">
              <% jobApplications.forEach(application => { %>
                  <div class="list-group-item application-item p-3 mb-2">
                      <div class="d-flex justify-content-between align-items-center mb-2">
                          <h6 class="mb-0"><%= application.freelancerId.name %></h6>
                          <% if (application.jobId) { %>
                              <span class="badge bg-primary rounded-pill"><%= application.jobId.title %></span>
                          <% } else { %>
                              <span class="badge bg-danger rounded-pill">[Job Deleted]</span>
                          <% } %>
                      </div>
                      <p class="application-info mb-2">
                          <i class="bi bi-envelope me-2"></i><%= application.freelancerId.email %>
                      </p>
                      <p class="budget mb-2">
                          <i class="bi bi-currency-rupee me-2"></i>Expected Budget: &#8377;<%= application.expectedBudget %>
                      </p>
                      <p class="proposal mb-3"><strong>Proposal:</strong> <%= application.proposal %></p>
                      <p class="availability mb-3">
                          <i class="bi bi-clock me-2"></i>Availability: <%= application.availability %>
                      </p>
                      <div class="d-flex justify-content-end">
                          <form method="POST" action="/acceptApplication/<%= application._id %>" class="me-2">
                              <button class="btn btn-sm btn-accept text-white">
                                  <i class="bi bi-check2 me-1"></i>Accept
                              </button>
                          </form>
                          <form method="POST" action="/rejectApplication/<%= application._id %>">
                              <button class="btn btn-sm btn-decline text-white">
                                  <i class="bi bi-x me-1"></i>Reject
                              </button>
                          </form>
                      </div>
                  </div>
              <% }); %>
          </div>
      <% } else { %>
          <div class="text-center mt-5">
              <i class="bi bi-briefcase-x fs-1 text-muted"></i>
              <p class="no-requests mt-3">No new job applications at the moment.</p>
          </div>
      <% } %>
  </div>
</div>

 
  <div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
       <div class="modal-content">
        <div class="modal-header flex-column text-center bg-white border-0">
          <i class="bi bi-person-circle fs-1 text-success"></i>
          <h2 class="mb-0 mt-2" style="color: #212529;">Please Login</h2>
          <p class="mb-0 mt-2 opacity-75" style="color: #495057;">Access your account to continue</p>
          <button type="button" class="btn-close position-absolute end-0 top-0 m-3" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form method="post" action="/login" class="needs-validation" novalidate>
            <div class="row g-3">
              <div class="col-md-12">
                <label for="inputusername4" class="form-label">Username</label>
                <input name="username" type="text" class="form-control"  placeholder="please enter your username" id="inputusername4" required>
              </div>

              <div class="col-md-12 position-relative">
                <label  for="inputPassword4" class="form-label">Password</label>
                <div class="input-group">
                    <input name="password" type="password" class="form-control" placeholder="Please enter your password" id="inputPassword4" required>
                    <span class="input-group-text bg-white" id="togglePassword" style="cursor: pointer;">
                        <i class="bi bi-eye-slash bold-icon"></i>
                    </span>
                </div>
              </div>

              <div class="col-12">
                <div class="form-check">
                  <input class="form-check-input" type="checkbox" value="" id="invalidCheck" required>
                  <label class="form-check-label" for="invalidCheck">
                    Agree to terms and conditions
                  </label>
                  <div class="invalid-feedback">
                    You must agree before submitting.
                  </div>
                </div>
              </div>

              <div class="d-grid gap-2 col-12 mx-auto">
                <button type="submit" class="btn styled-button">Login</button>
              </div>
              <div class="d-grid gap-2 col-12 mx-auto">
                <a href="/login-otp" type="submit" class="btn styled-button">
                  <i class="bi bi-key me-1"></i>
                  Login with OTP
                </a>
              </div>
              <div class="text-center mt-2" >
                <a href="/forgot-password" class="btn btn-link" style="color: #495057;">Forgot Password?</a>
              </div>
            </div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>